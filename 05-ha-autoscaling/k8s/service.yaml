apiVersion: v1
kind: Service
metadata:
  name: ha-app-svc
  namespace: production
spec:
  selector:
    app: ha-app
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP